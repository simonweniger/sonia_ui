{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "*/dist/**"],
      "env": [
        "REGISTRY_URL",
        "SCHEMA_URL"
      ]
    },
"clean": {
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "lint:staged": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}

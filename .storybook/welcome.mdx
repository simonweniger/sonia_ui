import {Meta} from "@storybook/addon-docs/blocks";
import {buttonVariants} from "../src";

<Meta title="Welcome" />

<br />

# Welcome 👋🏼

<br />

## SoniaUI Version 1 - Design system

<div className="max-w-xl text-base! text-foreground! [&>p]:text-base [&>p]:text-foreground">
  Here you can find the guidelines, components APIs and examples for Sonias Design System
</div>


<br />

export const currentDate = new Date();

export const isoDate = currentDate.toISOString().split("T")[0];

export const formattedDate = currentDate.toLocaleDateString("en-US", {
  month: "short",
  day: "numeric",
  year: "numeric",
});

<div className="[&>p]:text-default-400 block text-xs">
  Last updated on <time dateTime={isoDate}>{formattedDate}</time>
</div>
